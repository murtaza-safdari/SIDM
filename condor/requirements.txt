awkward==2.8.2
coffea==2025.5.0rc2
fastjet==3.4.3.1
hist==2.8.0
# llvmlite/numba arrive transitively via coffea. Condor workers build their venv
# from CMSSW_14_1_0_pre4, whose python is 3.9, and every numba from 0.61 on
# requires python >=3.10 -- a worker resolves numba 0.60.0 / llvmlite 0.43.0, so
# it cannot reach the numba 0.67.0 / llvmlite 0.49.0 pair that miscompiles
# coffea's GenPart children kernel. This bound is only a guard for a future
# CMSSW bump to python >=3.10; see the note in the top-level requirements.txt.
numba<0.67
numpy==1.26.4
PyYAML==6.0.2
setuptools==80.1.0
tabulate==0.9.0
vector==1.6.2
uproot==5.6.2