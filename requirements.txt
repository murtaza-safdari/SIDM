awkward==2.8.7
bokeh==3.9.1
coffea==2025.5.0rc2
dask==2025.3.0
fastjet==3.4.3.1
hist==2.9.0
# llvmlite/numba arrive transitively via coffea and are pinned deliberately:
# numba 0.67.0 + llvmlite 0.49.0 (both released 2026-08-11) miscompile coffea's
# GenPart children kernel -- "LLVM IR parsing error: invalid cast opcode for
# cast from 'i64' to 'ptr'" -- which crashes any processor run that touches
# gen-level objects. Unpin only after checking a full chain run.
llvmlite==0.47.0
matplotlib==3.10.9
mplhep==1.2.0
numba==0.65.1   # see the llvmlite note above
numpy==2.3.2
PyYAML==6.0.2
scipy==1.17.1
setuptools==80.1.0
tabulate==0.9.0
# uproot is imported directly (sidm/tools/file_census.py, condor/run_job.sh) as
# well as through coffea, and was left floating here while
# condor/requirements.txt already pinned it. 5.7.5 is the version CI has been
# resolving since 2026-07-04.
uproot==5.7.5
vector==1.6.3
xrootd==5.7.0
